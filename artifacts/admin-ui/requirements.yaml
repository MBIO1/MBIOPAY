packages:
  "framer-motion": "For page transitions, smooth modal appearances, and engaging micro-interactions."
  "date-fns": "For formatting timestamps and dates in the admin tables elegantly."
  "clsx": "Utility for constructing className strings conditionally."
  "tailwind-merge": "Utility to merge tailwind classes without style conflicts."

images: []

notes:
  - "Assuming /api/admin/me endpoint exists to check active session state."
  - "Assuming generic JSON error responses like { error: string } for 400/500s."
  - "Using generated mock data structures matching the provided spec if API endpoints are not yet fully implemented on the backend, allowing graceful degradation via Zod safeParse logging."
